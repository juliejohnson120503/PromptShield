# PromptShield AI — Dependencies
# Phase 1 Hybrid Detection Foundation

# ── Testing ────────────────────────────────────────────────────────────────
pytest>=7.4.0

# ── Phase 1: Hybrid Detection Foundation ──────────────────────────────────
# Structured / deterministic detection — Python standard library only (re).
# No external package required for regex detection.

# Microsoft Presidio — ML-backed PII detection framework
presidio-analyzer>=2.2.350

# spaCy — pretrained NER (en_core_web_sm model)
# After install, run:  python -m spacy download en_core_web_sm
spacy>=3.7.0

# GLiNER — Transformer-based open-vocabulary zero-shot NER
gliner>=0.2.0

# ── Future Phases (2-6) ────────────────────────────────────────────────────
# sentence-transformers>=2.3.0   # Phase 2 — Context Analysis
# pydantic>=2.6.0
# fastapi>=0.110.0               # Phase 6 — REST API
# uvicorn>=0.28.0
# httpx>=0.27.0
